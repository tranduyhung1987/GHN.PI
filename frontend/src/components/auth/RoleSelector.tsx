// src/components/auth/RoleSelector.tsx
export default function RoleSelector() {
  // TẮT TẠM ĐỂ TEST GIAO DIỆN MOBILE
  return null;

  // Nếu sau này muốn bật lại thì comment dòng trên và mở đoạn dưới
  /*
  if (import.meta.env.PROD) return null;
  // ... code role cũ của bạn
  */
}